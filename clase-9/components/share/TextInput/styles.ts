import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    container: {
        height: 40,
        width: '100%',
        borderColor: 'black',
        borderWidth: 1,
        marginTop: 20,
        padding: 10,
        borderRadius: 5,
        backgroundColor: '#efefef',
        color: '#0000cc',
    },
})